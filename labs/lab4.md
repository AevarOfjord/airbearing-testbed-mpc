# Lab 4 — Binary vs PWM vs mismatch

```bash
make lab4
```

Solenoid relaxation vs `--round-binary`, PWM fans, and a controller that believes `F_max` is twice the plant. Which error grows, and why should that number live in JSON rather than Python?
