Shipped example vehicles (`vehicles/`) and golden logs (`logs/`). Copy a vehicle JSON into `vehicles/` (or the editor) and calibrate `F_max`.
Compare offline: `airbearing compare --sim examples/logs/sim.csv --real examples/logs/hardware.csv`.
